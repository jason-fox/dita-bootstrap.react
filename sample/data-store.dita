<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic PUBLIC "-//OASIS//DTD DITA Topic//EN" "topic.dtd">
<topic id="data-store-service">
  <title>Data Store Service (data-store)</title>
  <shortdesc>Containerized file server and REST API for document set discovery, static AST JSON serving, and search index generation.</shortdesc>
  <prolog>
    <metadata>
      <keywords>
        <indexterm>data-store</indexterm>
        <indexterm>REST API</indexterm>
        <indexterm>Discovery</indexterm>
        <indexterm>MiniSearch</indexterm>
        <indexterm>Docker</indexterm>
      </keywords>
    </metadata>
  </prolog>
  <body>
    <section>
      <title>Service Overview</title>
      <p>The <b>data-store</b> service operates as the core content repository backend. It scans the shared data volume (<filepath>DATA_DIR</filepath>) for published documentation sets, serves JSON AST files, and builds full-text search indices.</p>
    </section>

    <section>
      <title>Key Capabilities</title>
      <ul>
        <li><b>Documentation Discovery</b> — Recursively scans <filepath>DATA_DIR</filepath> for subdirectories containing a <filepath>toc.json</filepath> manifest and exposes them via <filepath>GET /api/docs</filepath>.</li>
        <li><b>Static AST Asset Serving</b> — Serves compiled topic JSON files, image assets, and Table of Contents manifests under <filepath>/data/...</filepath>.</li>
        <li><b>Search Index Generation</b> — Generates an in-memory/JSON MiniSearch index (<filepath>search-index.json</filepath>) for each document set upon server startup.</li>
      </ul>
    </section>

    <section>
      <title>Container Deployment &amp; Configuration</title>
      <p>Deploy the service using Docker Compose or a standalone Docker container without requiring local package managers:</p>
      
      <codeblock outputclass="language-bash"># Launch data-store using Docker Compose:
docker compose up -d data-store

# Or run as a standalone Docker container:
docker run -d -p 4000:4000 \
  -v /path/to/data:/app/data \
  ghcr.io/jason-fox/ast-data-store:latest</codeblock>

      <p>Configuration Environment Variables:</p>
      <parml>
        <plentry>
          <pt><codeph>PORT</codeph></pt>
          <pd>HTTP port for the Express REST server (default: <codeph>4000</codeph>).</pd>
        </plentry>
        <plentry>
          <pt><codeph>DATA_DIR</codeph></pt>
          <pd>Directory path containing published documentation sets (default: <filepath>/app/data</filepath>).</pd>
        </plentry>
        <plentry>
          <pt><codeph>DEFAULT_LANGUAGE</codeph></pt>
          <pd>Fallback IETF BCP 47 language code for document set discovery (default: <codeph>en</codeph>).</pd>
        </plentry>
      </parml>
    </section>

    <section>
      <title>REST Endpoints</title>
      <ul>
        <li><codeph>GET /api/docs</codeph> — Returns a JSON array of all discovered documentation sets with OASIS metadata.</li>
        <li><codeph>GET /health</codeph> — Health check endpoint returning server status and active data directory path.</li>
        <li><codeph>GET /data/...</codeph> — Static file mount for <filepath>toc.json</filepath>, topic files, and search indices.</li>
      </ul>
    </section>
  </body>
</topic>
